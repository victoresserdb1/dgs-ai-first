# Dependências do Pipeline RAG NovaTech
# Versões fixadas para reprodutibilidade (NC-05)
# Gerado em: 2026-06-06 — ambiente Python 3.13
#
# Para instalar em ambiente limpo:
#   python -m venv venv
#   venv\Scripts\activate        (Windows) / source venv/bin/activate (Linux/Mac)
#   pip install -r requirements.txt
#
# Para atualizar este arquivo após instalar novos pacotes:
#   pip freeze > requirements.txt

# Dependências diretas
chromadb==1.5.9
sentence-transformers==5.5.1

# Dependências transitivas críticas (fixadas para estabilidade)
torch==2.12.0
numpy==2.4.6
huggingface-hub==1.18.0
tokenizers==0.22.2
